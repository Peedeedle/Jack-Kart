////////////////////////////////////////////////////////////
// File: PerceptionIDManager.cs
// Author: Jack Peedle
// Date Created: 13/05/22
// Last Edited By: Jack Peedle
// Date Last Edited: 13/05/22
// Brief:   
//////////////////////////////////////////////////////////// 

using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Perception;
using UnityEngine.Perception.GroundTruth;
using System;

public class PerceptionIDManager : MonoBehaviour
{
    
    //public List<string> gameobjectLabels;

    //public int AgentLabelInt;

    //public IdLabelConfig IdLabelConfig;

    //public List<string> labels = new List<string>();

    //RenderedObjectInfoLabeler

    //string ILabelEntry.label => label;

    //public int intTest;

    //public List<int> intList;

    ///public GameObject testGO;

    //public bool TryGetMatchingConfigurationEntry(Labeling labeling, out IdLabelEntry labelOut)

    ///public List<GameObject> IDGameObjectLists = new List<GameObject>();


    public void Start() {

        

        //labels.Add(IdLabelConfig.labelEntries.ToString());

        //intTest(IdLabelConfig.startingLabelId);

        //intList.Add(IdLabelConfig.GetInstanceID.ToString());

        ///testGO.GetInstanceID();

        //Debug.Log(IdLabelConfig.GetInstanceID);
        ///Debug.Log(testGO.GetInstanceID());


    }

    
    public void Update() {

        //IDGameObjectLists.Add(GameObject.com)

        //if (testGO.)
        //var labeling = testGO.GetComponent<Labeling>();
        /*

        Labeling[] LabelArray = (Labeling[])GameObject.FindObjectsOfType(typeof(Labeling));

        foreach (Labeling Labelss in LabelArray) {

            //LabelArray.Equals(Labelss.gameObject);

            IDGameObjectLists.Add(Labelss.gameObject);

            Debug.Log(LabelArray);

            Debug.Log(LabelArray.ToString());

        }
        */


        //labeling.labels.Add("Crate");

        //labeling.labels.


        /*
        if (labeling.instanceId == 1) {

            Debug.Log("1");

        }

        if (labeling.name == "pasta_lasagne_barilla") {

            Debug.Log("32141341");

        }


        if (labeling.instanceId == 2) {

            Debug.Log("2");

        }

        if (labeling.instanceId == 3) {

            Debug.Log("3");

        }

        if (labeling.instanceId == 4) {

            Debug.Log("4");

        }
        */



        /*

        bool Equals(IdLabelEntry other) {

            if (label == other.label) {

                return id == other.id;

            }

        }

        bool Equals(IdLabelEntry other) {

            if (label == other.label) {

                return id == other.id;

            }

            return false;
        } 

        public override bool Equals(object obj) {

        if (obj is IdLabelEntry) {

            IdLabelEntry other = (IdLabelEntry)obj;
            return Equals(other);

        }

        return false;
        }

        //IdLabelConfig.DoesLabelMatchAnEntry(AgentLabelInt);

        //if (IdLabelEntry)

        */
        
    }
    
}
